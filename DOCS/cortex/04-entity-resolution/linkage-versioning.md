# Linkage Versioning

## Version Dimensions
- linkage schema version,
- deterministic linking rules version,
- AI inference version (if used),
- processor version,
- replay version.

## Versioning Rules
- version tuple persisted on linkage/continuity/ambiguity outputs,
- version changes require replay comparability checks,
- breaking linkage schema changes require migration and replay strategy.
