# Replay‑aware reasoning law (Phase 06)

**Status:** constitutional law.

## Core obligation

Reasoning outputs MUST be functions of **(evidence bundle hash, extraction/reasoning rule pack version, declared permutation profile)** — same triple ⇒ same deterministic artifact ids where equality is claimed.

## Interaction with Phase 05

- **Async permutation:** causal summaries that ingest walk job order MUST declare dependency on **L‑EQ‑02** class posture from OCTS equivalence doctrine or mark `replay_degraded`.  
- **Index replay:** if derived index epoch participates, include `index_content_hash` receipt in lineage when required by `replay-equivalence-reasoning-spec.md`.

## Forbidden

Using wall‑clock “processing time” as causal evidence. Using nondeterministic iteration order over unordered sets without sorted key rule.
