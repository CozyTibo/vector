# Phase 06 — Anti‑goals & forbidden cognition classes

**Status:** constitutional doctrine (spec layer).  
**Purpose:** Bound Phase **06** so it cannot collapse into “AI understands the org.”

## Forbidden capabilities

- Embeddings, vector databases, semantic similarity, topic models, clustering for linkage.  
- LLM‑generated causal narratives, chain‑of‑thought as evidence, or “confidence” without `DeterministicConfidenceSource`.  
- Probabilistic matching, fuzzy graph expansion, latent variable inference.  
- Autonomous agents, planners, or self‑directed orchestration over tenant data.  
- Emotional, motivational, or performance scoring of people.  
- Hidden merge of contradictions without degradation receipts.

## Allowed substrate stance

- **Deterministic** temporal ordering from **temporal anchors** and connector‑native fields.  
- **Evidence‑bounded** causal edges: each edge cites raw ids, rule ids, and lineage per `execution_reconstruction_contracts.py`.  
- **Replay‑first:** any causal artifact must declare **replay posture** and survive permutation laws inherited from Phase **05** where applicable.

## Constitutional boundary (one sentence)

Phase **06** may only add **derivable structure already implicit in evidence + frozen rules** — never **new facts** from semantics.
