# Organizational continuity reasoning (Phase 06)

**Status:** constitutional doctrine.

---

## 1. Scope

How Phase **06** consumes **Phase 04** continuity (handles, links, projection export, replay jobs) as **evidence** for temporal–causal reconstruction — never as a semantic graph to “explore meaning.”

---

## 2. Rules

1. **Authoritative links** may justify **`TCRECausalEdge_v1`** records only when `link_authority` and temporal validity gates pass and **`underlying_coordination_edge_ids`** / sentinel rules per [`tcre-causal-edge-registry-v1.md`](./tcre-causal-edge-registry-v1.md) close.  
2. **Candidates / hints** never justify sole support for a causal edge — at most **`AMB‑BRIDGE‑weak`** (canonical id per [`ambiguity-registry-v1.md`](./ambiguity-registry-v1.md)).  
3. **OrgGraphProjectionV1** ingress may supply **structural** walk inputs; causal claims must still cite raw or org ledger lineage.

---

## 3. Handoff to Phase 05

Walk artifacts are **evidence**, not oracle: if walk **`replay_conflicted`**, causal reducer **must** emit **`replay_conflicted`** posture on dependent outputs per [`chronology-replay-legality-state-machine.md`](./chronology-replay-legality-state-machine.md).
