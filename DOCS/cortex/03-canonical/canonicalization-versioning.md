# Canonicalization Versioning

## Version Dimensions
- canonical schema version,
- deterministic extraction version,
- ontology mapping version,
- processor version,
- inference version (if AI-assisted),
- replay version context.

## Versioning Rules
- version tuple is persisted on all canonical outputs.
- version changes require replay compatibility assessment.
- breaking schema changes require migration/replay strategy.
